export type Theme = {
    colors: {
      primary: string
      secondary: string
      background: string
      text: string
    }
    fonts: {
      body: string
      heading: string
    }
    spacing: {
      small: string
      medium: string
      large: string
    }
  }